#include <stdio.h>
/* Count Characters in Input Version 1 */
main()
{
  long nc;
  
  nc = 0;
  while (getchar() !=EOF)
    ++nc;
    printf("%\d\n", nc);
}

#include <stdio.h>
/* Count Characters in Input Version 2 */
main()
{
  double nc;
  
  for (nc=0; getchar() != EOF; ++nc)
    ;
  printf("%
}
